import { ACTION_TYPE } from "../constants/action"

export const todos = (state = [], action) => {
    switch (action.type) {
        default:
            return state
    }
}

export const loading = (state = [], value) => value